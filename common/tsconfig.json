{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "typeRoots": ["../types", "../.yarn/types", "../.yarn/cache/**/*.types"],
    "rootDir": "../",
    "baseUrl": "../"
  }
}
